# This file is written by xdg-user-dirs-update
# If you want to change or add directories, just edit the line you're
# interested in. All local changes will be retained on the next run.
# Format is XDG_xxx_DIR="$HOME/yyy", where yyy is a shell-escaped
# homedir-relative path, or XDG_xxx_DIR="/yyy", where /yyy is an
# absolute path. No other format is supported.
# 
XDG_DESKTOP_DIR="$HOME/.desktop"
XDG_DOWNLOAD_DIR="$HOME/dump"
XDG_TEMPLATES_DIR="$HOME/"
XDG_PUBLICSHARE_DIR="/repo/public"
XDG_DOCUMENTS_DIR="/repo/docs"
XDG_MUSIC_DIR="/repo/audio"
XDG_PICTURES_DIR="/repo/photo"
XDG_VIDEOS_DIR="/repo/photo"

# XDG_DATA_HOME=/data  # DECISION:
# 1. Change XDG_DATA_HOME to /data?
# 2. Link /data to ~/.local/share (no, served data cannot be owned by user)
# 3. Keep separate (does .local/share contain any data of interest for backup?)

# XDG_CACHE_HOME=$HOME/.cache (default)
